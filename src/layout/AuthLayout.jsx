export default function AuthLayouts({ children }) {
    return (
        <div className='min-h-screen flex justify-center items-center font-poppins bg-stone-100'>
            {children}
        </div>
    );
}
